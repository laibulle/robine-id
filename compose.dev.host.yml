# PostgreSQL host access is part of the development contract and remains bound to loopback in the
# base model. Keeping this overlay valid preserves existing commands without changing networking.
services: {}
