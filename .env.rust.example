DATABASE_URL=postgres://robine_id:robine_id_dev@127.0.0.1:54329/robine_id
# Instead of DATABASE_URL, set DATABASE_URL_FILE to a mounted secret path.
DATABASE_MAX_CONNECTIONS=5
DATABASE_ACQUIRE_TIMEOUT_MS=5000
DATABASE_STATEMENT_TIMEOUT_MS=5000
KEY_ENCRYPTION_SECRET=replace-with-at-least-32-random-bytes
# Instead of KEY_ENCRYPTION_SECRET, set KEY_ENCRYPTION_SECRET_FILE.
# Temporary during a staged encryption-secret rotation only:
# KEY_ENCRYPTION_SECRET_PREVIOUS=replace-with-the-former-secret
ROBINE_ID_CONFIG=config/robine_id.json
ROBINE_ID_APPLICATIONS_DIR=config/applications
ROBINE_ID_RELOAD_INTERVAL=1000
DATABASE_CLEANUP_INTERVAL=3600
DRAIN_DELAY_MILLISECONDS=3000
SHUTDOWN_TIMEOUT_SECONDS=10
TRUST_PROXY_HEADERS=false
